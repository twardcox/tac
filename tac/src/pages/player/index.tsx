import { tacApi } from '@/server/api';

const Player = () => {
  // const dbConnection = await tacApi;
  // console.log('dbConnection: ', dbConnection);

  return <h1>Player Here</h1>;
};

export default Player;
